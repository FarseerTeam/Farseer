dependencies:
  override:
    - npm install -g bower
    - npm install -g grunt-cli
    - npm install
    - bower install
test:
  post:
    - grunt test
deployment:
  production:
    branch: production
    commands:
      - grunt buildcontrol:heroku
