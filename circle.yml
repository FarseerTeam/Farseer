dependencies:
  override:
    - npm install -g bower
    - npm install -g grunt-cli
    - npm install
    - bower install
test:
  post:
    - grunt test
deployment:
  production:
    branch: master
    commands:
      - grunt build
      - cd dist
      - git config user.email "rmurdock@pillartechnology.com"
      - git config user.name "Circle CI pretending to be Rob"
      - cd ..
      - grunt buildcontrol:heroku
