dependencies:
  override:
    - npm install -g bower
    - npm install -g grunt-cli
    - npm install
    - bower install
test:
  post:
    - grunt test
deployment:
  production:
    branch: master
    commands:
      - grunt build
      - grunt buildcontrol:heroku
