'use strict';

var page = require('./players-map.po');
var setup = require('../common/data-setup');

describe('The playersMap page has images for each player on it, and... ', function() {

	it('the images are retrieved from gravatar.', function(done) {

	});
});